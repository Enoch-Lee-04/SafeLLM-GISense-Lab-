# Scripts Directory

This directory contains all Python scripts organized by purpose:

- `training/` - Training scripts for different models
- `evaluation/` - Evaluation and testing scripts  
- `utils/` - Utility scripts for inference and deployment
- `cleanup/` - Repository maintenance and cleanup scripts
